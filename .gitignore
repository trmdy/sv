# bv (beads viewer) local config and caches
.bv/

# Rust build artifacts
/target/
**/*.rs.bk
Cargo.lock

# sv local state (per-workspace)
.sv/

# sv tasks temp/lock files
.tasks/*.lock
.tasks/*.tmp

# fmail
.fmail
